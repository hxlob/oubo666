export default{ //把当前js文件以文件名称的形式进行导出
    items:[
        //存放每个购物项
        /**
         * id,
         * image,
         * title,
         * price
         * num,
         * checked,
         */
    ],
    total:900, //总价
    allchecked:true,
}